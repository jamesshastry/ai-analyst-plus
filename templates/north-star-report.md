# North Star Report — {PRODUCT} ({PERIOD})

_Generated by Claude Code from `{DATA_SOURCE}` · North Star: **{NSM_NAME}**_

## Headline

{NSM_NAME} {DIRECTION} **{HEADLINE_CHANGE}** over {PERIOD} — from **{NSM_START}**
to **{NSM_END}** {NSM_UNIT}{OPTIONAL_PEAK}. {ONE_LINE_SURFACE_READ}

## Input breakdown (BDEF)

Decompose the NSM into its four levers. Fill the trend from the data and write a
one-phrase read per row (▲ up / ▬ flat / ▽ down).

| Lever | Input | {PERIOD} trend | Read |
|-------|-------|-----------|------|
| **Breadth** | {BREADTH_INPUT} | {BREADTH_TREND} | {BREADTH_READ} |
| **Frequency** | {FREQUENCY_INPUT} | {FREQUENCY_TREND} | {FREQUENCY_READ} |
| **Depth** | {DEPTH_INPUT} | {DEPTH_TREND} | {DEPTH_READ} |
| **Efficiency** | {EFFICIENCY_INPUT} | {EFFICIENCY_TREND} | {EFFICIENCY_READ} |

## Root cause

Which input(s) actually moved the NSM, and which did not. Name the structural
risk the decomposition exposes — especially when it contradicts the headline.
Ground every claim in a number from the data.

## Recommendation

1. **{HIGHEST_LEVERAGE_INPUT}** — the input to move next, and why it beats the
   obvious alternative.
2. **{SECONDARY_FINDING}** — a lever that looks promising but the data says isn't
   working yet (report it as a finding, not a solution).
3. **Guardrail: {GUARDRAIL_INPUT}** — the input that could degrade if you chase
   the NSM naively.

_The metric gave you the headline. The decomposition + judgment gave you the
decision. State plainly what the human saw that the number didn't._
